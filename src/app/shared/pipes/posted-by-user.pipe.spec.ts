import { PostedByUserPipe } from './posted-by-user.pipe';

describe('PostedByUserPipe', () => {
  it('create an instance', () => {
    const pipe = new PostedByUserPipe();
    expect(pipe).toBeTruthy();
  });
});
